# 개인 문서 파일 (PDF, Word 등)
data/
# 환경 변수 파일 (API 키 등 민감 정보)
.env
ingestion_script/.env
ingestion_script/extracted_md/
ingestion_script/processed_files.json

# 자동 생성되는 임시 및 데이터 파일
ingestion_script/extracted_md/
ingestion_script/processed_files.json

# 파이썬 가상 환경 (선택 사항이지만 권장)
venv/
__pycache__/
*.pyc
